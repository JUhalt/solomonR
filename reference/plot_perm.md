# Plot permutation distribution for a Solomon contrast

Plot permutation distribution for a Solomon contrast

## Usage

``` r
plot_perm(perm)
```

## Arguments

- perm:

  A list returned by perm_solomon(..., return_dist = TRUE) containing
  `z_perm` and `z_obs`.

## Value

A ggplot object
