% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solomon_sem_latent.R
\name{fit_solomon_sem_latent}
\alias{fit_solomon_sem_latent}
\title{Latent SEM for Solomon Four-Group designs (POST means; optional latent ANCOVA)}
\usage{
fit_solomon_sem_latent(
  data,
  post_items,
  treat,
  pretested,
  pre_items = NULL,
  invariance_post = c("scalar", "metric", "configural"),
  ancova = FALSE,
  invariance_pre = c("scalar", "metric", "configural"),
  estimator = "MLR",
  std_lv = TRUE
)
}
\arguments{
\item{data}{data.frame containing all variables}

\item{post_items}{character vector of posttest item names (required)}

\item{treat}{0/1 numeric (or logical) treatment indicator (length nrow(data))}

\item{pretested}{0/1 numeric (or logical) pretest indicator (length nrow(data))}

\item{pre_items}{character vector of pretest item names (for ANCOVA branch)}

\item{invariance_post}{one of "configural","metric","scalar" (default "scalar")}

\item{ancova}{logical; if TRUE, also fit latent ANCOVA in pretested groups}

\item{invariance_pre}{one of "configural","metric","scalar" for pretested branch}

\item{estimator}{lavaan estimator, default "MLR" (robust)}

\item{std_lv}{logical; if TRUE (default), std.lv=TRUE to put factors on SD=1 scale}
}
\value{
An object of class \code{solomon_sem_latent} with:
\itemize{
\item \code{fit_post}: lavaan object for the 4-group POST model
\item \code{effects_post}: data.frame of ATE, Sens, Pre_Eff, Unpre_Eff on latent POST
\item \code{fitmeasures_post}: named vector (CFI, RMSEA, SRMR, df)
\item \code{fit_pre} (optional): lavaan object for pretested latent ANCOVA
\item \code{effects_pre} (optional): data.frame with \code{Pre_Eff} on latent POST (pretested)
\item \code{fitmeasures_pre} (optional)
}
}
\description{
This function provides a fully latent analysis path:
(A) A 4-group SEM that defines a latent POST factor from multiple
indicators and estimates group-specific latent means for
P1, P0, U1, and U0. From these we compute ATE, Sens (Pretest x Treat),
and simple effects on the latent outcome.
(B) Optionally, a 2-group SEM in \strong{pretested} groups only (P1 vs P0)
with a latent PRE factor and latent POST factor, fitting a latent
ANCOVA (POST ~ PRE), and reporting the pretested simple effect.
}
\details{
Measurement invariance for POST can be set via \code{invariance_post}:
\itemize{
\item "configural" (default): equal form only
\item "metric": equal loadings
\item "scalar": equal loadings + intercepts (supports mean comparisons)
}

For the pretested ANCOVA branch, \code{invariance_pre} applies across P1/P0.
}
