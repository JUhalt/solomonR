% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solomon_power.R
\name{power_solomon}
\alias{power_solomon}
\title{Power simulation for Solomon designs}
\usage{
power_solomon(
  n = 50,
  delta = 0.3,
  rho = 0.5,
  sens = 0,
  sigma = 1,
  sims = 2000,
  stouffer = TRUE,
  alpha = 0.05,
  seed = NULL
)
}
\arguments{
\item{n}{Cell sizes: a single number used for all four cells, or four
sizes given as a list or vector with elements \code{n1} (pretested treatment),
\code{n2} (pretested control), \code{n3} (unpretested treatment), and \code{n4}
(unpretested control).}

\item{delta}{Treatment effect among unpretested participants, on the
posttest scale.}

\item{rho}{Pretest-posttest correlation among pretested participants.}

\item{sens}{Sensitization: the additional treatment effect among pretested
participants (0 = none).}

\item{sigma}{Posttest residual standard deviation in all cells.}

\item{sims}{Number of Monte Carlo replications.}

\item{stouffer}{Logical; if \code{TRUE}, also report the historical Test I
rejection rate, evaluated one-tailed (treatment > control) as in
\code{\link[=fit_solomon_classic]{fit_solomon_classic()}}.}

\item{alpha}{Significance level. Default is 0.05.}

\item{seed}{Optional integer seed. The global random number state is
restored afterwards.}
}
\value{
A data frame with one row per test, giving the estimand, the test
used, the true effect, the rejection rate and its Monte Carlo standard
error, the replications that produced a usable fit, the number of
failures, and \code{alpha}. Test I rows report \code{NA} for \code{true_effect}, because
the combination targets a directional hypothesis rather than a single
contrast, and all Test I values are \code{NA} when \code{stouffer = FALSE}.
}
\description{
Simulates normally distributed Solomon four-group data and reports the
rejection rate of each Solomon test at the chosen \code{alpha}, with its Monte
Carlo standard error.
}
\details{
Every participant has a latent baseline drawn from a standard normal
distribution, which only pretested participants observe, so structural
pretest absence is part of the design rather than missing data. The
posttest residual standard deviation is \code{sigma} in every cell, and the
pretest-posttest correlation among pretested participants is \code{rho}. The
treatment effect is \code{delta} among unpretested participants and
\code{delta + sens} among pretested participants, so the equal-weighted average
treatment effect is \code{delta + sens / 2}.

The reported \code{power} is a rejection rate: when \code{true_effect} is zero it
estimates the Type I error rather than power.

Tests are computed from the same functions users would call, so reported
power reflects the package's default inference: \code{fit_solomon_glm()} with
HC3 standard errors and t reference distributions, the 2x2 ANOVA
interaction, and the historical one-tailed Test I (Braver & Braver, 1988).
}
\section{Validation}{

A pre-specified simulation study of 126 scenarios and 315,000 replications
checked this function against normal-theory benchmarks. The protocol and
its amendment were posted to issue #18 before any results were examined,
and the article "Validating power_solomon()" on the package website reports
the study in full.
\itemize{
\item The 2x2 ANOVA interaction agreed with its analytic benchmark in every
scenario, with mean differences of 0.002 or less.
\item Test I held its nominal size under the complete null, rejecting between
4.4\% and 5.6\% of the time at alpha = .05.
\item Rejection rates were invariant to the residual scale, and no fit failed.
\item Rejection rates from the unified GLM are conservative in small samples,
because the HC3 standard errors the package uses by default are
conservative there. Type I error averaged 0.041 with 10 participants per
cell and 0.049 with 100, and simulated power fell below the normal-theory
benchmark by 0.030 on average with 10 per cell (up to 0.083 in one
scenario), 0.017 with 20, and 0.003 with 100.
}

With 20 or fewer participants per cell, treat GLM-based power as a
conservative figure rather than an exact one.
}

\examples{
power_solomon(n = 30, delta = 0.5, sens = 0.2, sims = 50, seed = 1)

}
\references{
Braver, M. W., & Braver, S. L. (1988). Statistical treatment of the
Solomon four-group design: A meta-analytic approach. \emph{Psychological
Bulletin, 104}(1), 150-154.

Morris, T. P., White, I. R., & Crowther, M. J. (2019). Using simulation
studies to evaluate statistical methods. \emph{Statistics in Medicine, 38}(11),
2074-2102.
}
