% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solomon_sem.R
\name{fit_solomon_sem}
\alias{fit_solomon_sem}
\title{SEM analysis for Solomon Four-Group designs (mean-structure; optional ANCOVA)}
\usage{
fit_solomon_sem(
  y_post,
  treat,
  pretested,
  y_pre = NULL,
  equal_var = FALSE,
  ancova = FALSE,
  estimator = "MLR"
)
}
\arguments{
\item{y_post}{numeric posttest}

\item{treat}{0/1 treatment}

\item{pretested}{0/1 pretest indicator}

\item{y_pre}{optional pretest score (required if ancova = TRUE)}

\item{equal_var}{logical; if TRUE, constrain posttest variances equal across groups}

\item{ancova}{logical; if TRUE, fit ANCOVA in pretested groups only (P1 vs P0)}

\item{estimator}{lavaan estimator (default "MLR")}
}
\description{
Two modes:
\enumerate{
\item mean-structure (default): 4-group SEM estimating posttest means for P1, P0, U1, U0,
and reporting ATE, Sens (Pretest×Treatment), Pre_Eff, Unpre_Eff.
\item ancova = TRUE: restricts to pretested groups (P1, P0) and fits y_post ~ beta*y_pre
with group means; reports the pretested simple effect (Pre_Eff). This avoids
structural missingness of y_pre in U1/U0 and matches Huck & Sandler.
}
}
