% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solomon_sem.R
\name{fit_solomon_sem}
\alias{fit_solomon_sem}
\title{SEM analysis for Solomon Four-Group designs (mean-structure; optional ANCOVA)}
\usage{
fit_solomon_sem(
  y_post,
  treat,
  pretested,
  y_pre = NULL,
  equal_var = FALSE,
  ancova = FALSE,
  estimator = "MLR",
  conf_level = 0.95
)
}
\arguments{
\item{y_post}{numeric posttest}

\item{treat}{0/1 (or logical) treatment indicator}

\item{pretested}{0/1 (or logical) pretest indicator}

\item{y_pre}{optional pretest score (required if ancova = TRUE)}

\item{equal_var}{logical; if TRUE, constrain posttest variances equal across groups}

\item{ancova}{logical; if TRUE, fit ANCOVA in pretested groups only (P1 vs P0)}

\item{estimator}{lavaan estimator (default "MLR")}

\item{conf_level}{confidence level for intervals (default 0.95)}
}
\description{
Two modes:
\enumerate{
\item mean-structure (default): 4-group SEM estimating posttest means for P1, P0, U1, U0,
and reporting ATE, Sens (Pretest×Treatment), Pre_Eff, Unpre_Eff.
\item ancova = TRUE: restricts to pretested groups (P1, P0) and fits y_post ~ beta*y_pre
with group means; reports the pretested simple effect (Pre_Eff). This avoids
structural missingness of y_pre in U1/U0 and matches Huck & Sandler.
}
}
\details{
The four-group mean-structure model is saturated, so its global fit
indices are not diagnostic. Its contrasts are unadjusted posttest mean
differences, whereas the ANCOVA mode adjusts for the pretest within the
pretested groups.

Tests and confidence intervals for the contrasts are lavaan's Wald
results, which use a large-sample normal reference distribution.
}
\references{
Huck, S. W., & Sandler, H. M. (1973). A note on the Solomon 4-group
design: Appropriate statistical analyses. \emph{The Journal of Experimental
Education, 42}(2), 54-55.

Rosseel, Y. (2012). lavaan: An R package for structural equation
modeling. \emph{Journal of Statistical Software, 48}(2), 1-36.
}
