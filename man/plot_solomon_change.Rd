% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solomon_plot_change.R
\name{plot_solomon_change}
\alias{plot_solomon_change}
\title{Pretest-to-posttest change in a Solomon design}
\usage{
plot_solomon_change(
  y_post,
  treat,
  pretested,
  y_pre,
  show_individuals = FALSE,
  conf_level = 0.95
)
}
\arguments{
\item{y_post}{Numeric posttest scores.}

\item{treat}{Treatment indicator coded 0 = control and 1 = treatment.}

\item{pretested}{Pretest indicator coded 0 = unpretested and 1 = pretested.}

\item{y_pre}{Numeric pretest scores, missing by design for unpretested
participants.}

\item{show_individuals}{Logical; if \code{TRUE}, draw each pretested
participant's change as a faint line behind the means. Default \code{FALSE}.}

\item{conf_level}{Confidence level for the intervals, which use the t
distribution with n - 1 degrees of freedom. Default is 0.95.}
}
\value{
A ggplot object.
}
\description{
Draws mean pretest and posttest scores for the two pretested groups,
joined to show change, alongside posttest means for the two unpretested
groups. The unpretested groups appear at posttest only: their missing
pretest is the experimental manipulation (Solomon, 1949), not missing data,
and the figure labels it that way.
}
\details{
Trajectories use pretested participants with both scores observed.
Pretested participants whose pretest is incidentally missing are left out
of the trajectories and counted in the caption, and are never imputed; see
\code{\link[=check_solomon_missing]{check_solomon_missing()}}.

A change reading of this figure applies only to the pretested groups. The
Solomon contrasts the package reports compare posttests, adjusting for the
pretest where one exists; see \code{\link[=compare_solomon_methods]{compare_solomon_methods()}} for the estimand
behind each analysis.
}
\examples{
with(solomon_example, plot_solomon_change(y_post, treat, pretested, y_pre))

}
\references{
Solomon, R. L. (1949). An extension of control group design.
\emph{Psychological Bulletin, 46}(2), 137-150.
}
