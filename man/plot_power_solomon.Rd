% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solomon_plot_power.R
\name{plot_power_solomon}
\alias{plot_power_solomon}
\title{Power curves for a Solomon design}
\usage{
plot_power_solomon(
  n = seq(10, 150, by = 10),
  delta = 0.3,
  sens = 0,
  rho = 0.5,
  sigma = 1,
  alpha = 0.05,
  estimand = c("ate", "sensitization", "pretested", "unpretested"),
  allocation = c(1, 1, 1, 1),
  target = 0.8,
  method = c("analytic", "simulation"),
  sims = 500,
  seed = NULL
)
}
\arguments{
\item{n}{Sizes of the smallest cell to plot. Other cells follow
\code{allocation}.}

\item{delta, sens, rho}{Treatment effect among unpretested participants,
sensitization, and pretest-posttest correlation, as in \code{\link[=power_solomon]{power_solomon()}}.
At most one of them may have more than one value; that one is shown by
color.}

\item{sigma}{Posttest residual standard deviation in all cells.}

\item{alpha}{Two-sided significance level. Default is 0.05.}

\item{estimand}{Estimands to plot: any of \code{"ate"}, \code{"sensitization"},
\code{"pretested"}, and \code{"unpretested"}.}

\item{allocation}{Relative cell sizes for \code{n1} to \code{n4}, as in
\code{\link[=plan_solomon]{plan_solomon()}}.}

\item{target}{Target power marked on the figure. Default is 0.80. Use
\code{NULL} to omit the target line and the planned designs.}

\item{method}{\code{"analytic"} (default) or \code{"simulation"}.}

\item{sims}{Replications per point when \code{method = "simulation"}.}

\item{seed}{Optional seed for \code{method = "simulation"}.}
}
\value{
A ggplot object.
}
\description{
Draws power against sample size for each Solomon estimand, so the effect of
cell size, effect size, pretest-posttest correlation, and sensitization on
a planned study can be seen rather than read from a table. A horizontal
line marks the target power, and each curve is annotated with the design
\code{\link[=plan_solomon]{plan_solomon()}} returns for that target.
}
\details{
Curves use the normal-theory power calculations validated for
\code{\link[=power_solomon]{power_solomon()}} by default. With \code{method = "simulation"}, each point is
the rejection rate of the package's own GLM test from \code{\link[=power_solomon]{power_solomon()}},
shown with a band of two Monte Carlo standard errors.

Power differs sharply between estimands: the sensitization contrast has
four times the sampling variance of the average treatment effect, so its
curve rises far more slowly. The figure is faceted by estimand to keep that
difference visible.
}
\examples{
plot_power_solomon(n = seq(10, 200, by = 10), delta = 0.4, sens = 0.2)
plot_power_solomon(n = seq(10, 200, by = 10), delta = 0.4, rho = c(0, 0.5, 0.8),
                   estimand = c("ate", "pretested"))

}
\seealso{
\code{\link[=power_solomon]{power_solomon()}}, \code{\link[=plan_solomon]{plan_solomon()}}
}
